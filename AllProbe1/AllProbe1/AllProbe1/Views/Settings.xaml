<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AllProbe1.Views.Settings"
             Title="Settings"
             BackgroundColor="White">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical" Padding="10">
            <StackLayout Orientation="Horizontal">
                <Label x:Name="lblLogout" Text="Logout" FontSize="Medium" VerticalOptions="Start" HorizontalOptions="StartAndExpand"/>
                <Image x:Name="imgLogout" VerticalOptions="Start" HorizontalOptions="End" Source="@drawable/logout.png"/>
            </StackLayout>
            <Label Text="Min. severity of events:" VerticalTextAlignment="Start" HorizontalTextAlignment="Start"/>
            <Picker Title="Show alerts from this severity or worse:" x:Name="pkSeverity" SelectedIndexChanged="SelectedItem_handler"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>